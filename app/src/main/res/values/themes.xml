<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--    <style name="Theme.MySerbianWords" parent="android:Theme.Material.Light.NoActionBar" />-->
    <!-- Base application theme. -->
    <style name="Base.Theme.MySerbianWords" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MySerbianWords" parent="Base.Theme.MySerbianWords">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:windowBackground">@color/white</item>

    </style>


    <style name="Theme.MySplashScreen" parent="Theme.SplashScreen">

        <item name="windowSplashScreenBackground">@color/splash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/baseline_thumb_up_alt_24</item>
        <item name="postSplashScreenTheme">@style/Theme.SplashScreenJs</item>
    </style>
    <style name="Theme.SplashScreenJs" parent="android:Theme.Material.Light.NoActionBar"></style>


    <style name="Theme.AppCompat.DayNight.DarkActionBar" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
